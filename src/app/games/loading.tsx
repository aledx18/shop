import Spinner from '../components/Spinner/Spinner'

export default function Loading() {
  return (
    <div className='flex h-screen container items-center justify-center'>
      <Spinner />
    </div>
  )
}
